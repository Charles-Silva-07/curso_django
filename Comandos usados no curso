Curso Python Pro
pip

name

pipenv install django-s3-folder-storage

ENVIAR UPLOAD DE ARQUIVOS SEM PERGUNTAR DEVIDO O NO-INPUT = mng collectstatic --no-input

SETAR AS CONFIGURAÇÕES NO HEROKU DO AWS=  heroku config:set -a django-0147

FOI USADA EM AULAS ANTERIORES PARA EVIAR A COLETA DE AQUVOS STATIC AGOAR
VAOMOS DESABILITAR = heroku config:unset -a django-0147 DISABLE_COLLECTSTATIC

pipenv update = atualiza as dependencias
pipenv sync = gera as dependencias de desenvolvimento do pipfilelock
pipenv install =
pipen uninstall =

#682256411

BANCO DE DADOS
* Instalação de Postgres com Docker

docker --help = Verificar comandos do docker

PARA RODAR
cd PycharmProjects/curso-django
dentro da pasta do projeto roda
docker-compose up


#version: '3.8'
#
#services:
#
#  db:
#    image: postgres
#    container_name: testedb
#    ports:
#      - "5434:5432"
#    tty: true
#    volumes:
#      - .docker/.pgdata:/var/lib/postgresql/data
#    environment:
#      - POSTGRES_DB=testedb
#      - POSTGRES_USER=postgres
#      - POSTGRES_PASSWORD=postgres
#      - POSTGRES_HOST=localhost

mng changepassword
pipenv shell ativa o ambiente virtual
python manage.py createsuperuser
Reaproveitando Apps de Terceiros ok
python manage.py migrate
python

CRIAR CHAVE SSH ssh-keygen -t rsa
